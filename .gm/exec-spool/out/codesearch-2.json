{"data":{"bm25_hits":[],"commits":[],"mode":"dual","vector_hits":[{"key":"ci-a6d4ca2b-44e75b5-0","symbol":{"kind":"impl_item","line_end":131,"line_start":97,"name":"","path":"crates/resound-core/src/irmae.rs"},"text":"crates/resound-core/src/irmae.rs:97:131 \nimpl IrmaeDecoder {\n    pub fn new(\n        latent_dim: usize,\n        hidden_dim: usize,\n        output_dim: usize,\n        num_res: usize,\n        vb: VarBuilder,\n    ) -> Result<Self> {\n        let cfg = Conv1dConfig {\n            padding: 1,\n            ..Default::default()\n        };\n        let stem = conv1d(latent_dim, hidden_dim, 3, cfg, vb.pp(\"0\"))?;\n        let mut res_blocks = Vec::with_capacity(num_res);\n        for i in 0..num_res {\n            res_blocks.push(ResBlock1d::new(hidden_dim, &[1, 2, 4, 8], vb.pp((1 + i).to_string()))?);\n        }\n        let cfg1 = Conv1dConfig::default();\n        let out_idx = 1 + num_res;\n        let out_conv = conv1d(hidden_dim, output_dim, 1, cfg1, vb.pp(out_idx.to_string()))?;\n        Ok(Self {\n            stem,\n            res_blocks,\n            out_conv,\n        })\n    }\n\n    pub fn forward(&self, x: &Tensor) -> Result<Tensor> {\n        let mut h = self.stem.forward(x)?;\n        for block in &self.res_blocks {\n            h = block.forward(&h)?;\n        }\n        self.out_conv.forward(&h)\n    }\n}"},{"key":"ci-7fd1ca3e-82c6bd8f-0","symbol":{"kind":"impl_item","line_end":104,"line_start":32,"name":"","path":"crates/resound-core/src/stft.rs"},"text":"crates/resound-core/src/stft.rs:32:104 \nimpl Stft {\n    pub fn new(n_fft: usize, hop_length: usize, win_length: usize) -> Self {\n        let mut planner = RealFftPlanner::<f32>::new();\n        Stft {\n            n_fft,\n            hop_length,\n            window: centered_window(n_fft, win_length),\n            planner_fwd: planner.plan_fft_forward(n_fft),\n            planner_inv: planner.plan_fft_inverse(n_fft),\n        }\n    }\n\n    fn padded(&self, wav: &[f32]) -> Vec<f32> {\n        let pad = self.n_fft / 2;\n        let mut out = vec![0.0f32; pad + wav.len() + pad];\n        out[pad..pad + wav.len()].copy_from_slice(wav);\n        out\n    }\n\n    pub fn num_frames(&self, wav_len: usize) -> usize {\n        let padded_len = wav_len + self.n_fft;\n        1 + (padded_len - self.n_fft) / self.hop_length\n    }\n\n    pub fn forward(&self, wav: &[f32]) -> Vec<Vec<Complex32>> {\n        let padded = self.padded(wav);\n        let n_frames = self.num_frames(wav.len());\n        let mut frames = Vec::with_capacity(n_frames);\n        let mut scratch = self.planner_fwd.make_scratch_vec();\n        for i in 0..n_frames {\n            let start = i * self.hop_length;\n            let mut input = self.planner_fwd.make_input_vec();\n            for j in 0..self.n_fft {\n                let sample = padded.get(start + j).copied().unwrap_or(0.0);\n                input[j] = sample * self.window[j];\n            }\n            let mut output = self.planner_fwd.make_output_vec();\n            self.planner_fwd\n                .process_with_scratch(&mut input, &mut output, &mut scratch)\n                .unwrap();\n            frames.push(output);\n        }\n        frames\n    }\n\n    pub fn inverse(&self, frames: &[Vec<Complex32>], out_len: usize) -> Vec<f32> {\n        let pad = self.n_fft / 2;\n        let total_len = pad + out_len + pad + self.n_fft;\n        let mut out = vec![0.0f32; total_len];\n        let mut win_sum = vec![0.0f32; total_len];\n        let mut scratch = self.planner_inv.make_scratch_vec();\n        for (i, frame) in frames.iter().enumerate() {\n            let start = i * self.hop_length;\n            let mut input = self.planner_inv.make_input_vec();\n            input.copy_from_slice(frame);\n            let mut output = self.planner_inv.make_output_vec();\n            self.planner_inv\n                .process_with_scratch(&mut input, &mut output, &mut scratch)\n                .unwrap();\n            let norm = 1.0 / self.n_fft as f32;\n            for j in 0..self.n_fft {\n                out[start + j] += output[j] * norm * self.window[j];\n                win_sum[start + j] += self.window[j] * self.window[j];\n            }\n        }\n        for i in 0..total_len {\n            if win_sum[i] > 1e-8 {\n                out[i] /= win_sum[i];\n            }\n        }\n        out[pad..pad + out_len].to_vec()\n    }\n}"},{"key":"ci-7a783dc9-e1c20a73-0","symbol":{"kind":"impl_item","line_end":174,"line_start":145,"name":"","path":"crates/resound-core/src/cfm.rs"},"text":"crates/resound-core/src/cfm.rs:145:174 \nimpl CfmSolver {\n    pub fn new(nfe: usize) -> Self {\n        CfmSolver { n_steps: nfe / 2 }\n    }\n\n    pub fn sample(&self, wn: &WaveNet, cond: &Tensor, psi0: &Tensor, device: &Device) -> Result<Tensor> {\n        let mut x = psi0.clone();\n        let raw_times: Vec<f64> = (0..=self.n_steps)\n            .map(|i| i as f64 / self.n_steps as f64)\n            .collect();\n        let mapped_times: Vec<f64> = raw_times.iter().map(|&t| time_mapping(t)).collect();\n\n        for i in 0..self.n_steps {\n            let t0 = mapped_times[i];\n            let t1 = mapped_times[i + 1];\n            let dt = t1 - t0;\n            let t_mid = t0 + dt / 2.0;\n\n            let t0_tensor = Tensor::from_vec(vec![t0 as f32], 1, device)?;\n            let v0 = wn.forward(&x, cond, &t0_tensor)?;\n\n            let x_mid = (&x + (v0.clone() * (dt / 2.0))?)?;\n            let t_mid_tensor = Tensor::from_vec(vec![t_mid as f32], 1, device)?;\n            let v_mid = wn.forward(&x_mid, cond, &t_mid_tensor)?;\n\n            x = (&x + (v_mid * dt)?)?;\n        }\n        Ok(x)\n    }\n}"},{"key":"ci-7a783dc9-e1c20a73-1","symbol":{"kind":"function_item","line_end":173,"line_start":150,"name":"sample","path":"crates/resound-core/src/cfm.rs"},"text":"crates/resound-core/src/cfm.rs:150:173 sample\n    pub fn sample(&self, wn: &WaveNet, cond: &Tensor, psi0: &Tensor, device: &Device) -> Result<Tensor> {\n        let mut x = psi0.clone();\n        let raw_times: Vec<f64> = (0..=self.n_steps)\n            .map(|i| i as f64 / self.n_steps as f64)\n            .collect();\n        let mapped_times: Vec<f64> = raw_times.iter().map(|&t| time_mapping(t)).collect();\n\n        for i in 0..self.n_steps {\n            let t0 = mapped_times[i];\n            let t1 = mapped_times[i + 1];\n            let dt = t1 - t0;\n            let t_mid = t0 + dt / 2.0;\n\n            let t0_tensor = Tensor::from_vec(vec![t0 as f32], 1, device)?;\n            let v0 = wn.forward(&x, cond, &t0_tensor)?;\n\n            let x_mid = (&x + (v0.clone() * (dt / 2.0))?)?;\n            let t_mid_tensor = Tensor::from_vec(vec![t_mid as f32], 1, device)?;\n            let v_mid = wn.forward(&x_mid, cond, &t_mid_tensor)?;\n\n            x = (&x + (v_mid * dt)?)?;\n        }\n        Ok(x)\n    }"},{"key":"ci-f9eca429-6800c6d1-0","symbol":{"kind":"impl_item","line_end":171,"line_start":88,"name":"","path":"crates/resound-core/src/unet.rs"},"text":"crates/resound-core/src/unet.rs:88:171 \nimpl UNet {\n    pub fn new(\n        input_dim: usize,\n        output_dim: usize,\n        hidden_dim: usize,\n        num_blocks: usize,\n        num_middle_blocks: usize,\n        vb: VarBuilder,\n    ) -> Result<Self> {\n        let cfg3 = Conv2dConfig {\n            padding: 1,\n            ..Default::default()\n        };\n        let input_proj = conv2d(input_dim, hidden_dim, 3, cfg3, vb.pp(\"input_proj\"))?;\n\n        let mut encoder_blocks = Vec::with_capacity(num_blocks);\n        let mut dim = hidden_dim;\n        let enc_vb = vb.pp(\"encoder_blocks\");\n        for i in 0..num_blocks {\n            let next_dim = dim * 2;\n            encoder_blocks.push(UNetBlock::new(dim, next_dim, Some(0.5), enc_vb.pp(i))?);\n            dim = next_dim;\n        }\n\n        let mut middle_blocks = Vec::with_capacity(num_middle_blocks);\n        let mid_vb = vb.pp(\"middle_blocks\");\n        for i in 0..num_middle_blocks {\n            middle_blocks.push(UNetBlock::new(dim, dim, None, mid_vb.pp(i))?);\n        }\n\n        let mut decoder_blocks = Vec::with_capacity(num_blocks);\n        let dec_vb = vb.pp(\"decoder_blocks\");\n        for i in 0..num_blocks {\n            let next_dim = dim / 2;\n            decoder_blocks.push(UNetBlock::new(dim, next_dim, Some(2.0), dec_vb.pp(i))?);\n            dim = next_dim;\n        }\n\n        let head_vb = vb.pp(\"head\");\n        let head_conv1 = conv2d(hidden_dim, hidden_dim, 3, cfg3, head_vb.pp(\"0\"))?;\n        let cfg1 = Conv2dConfig::default();\n        let head_conv2 = conv2d(hidden_dim, output_dim, 1, cfg1, head_vb.pp(\"2\"))?;\n\n        Ok(Self {\n            input_proj,\n            encoder_blocks,\n            middle_blocks,\n            decoder_blocks,\n            head_conv1,\n            head_conv2,\n            num_blocks,\n        })\n    }\n\n    fn pad_to_fit(&self, x: &Tensor) -> Result<(Tensor, usize, usize)> {\n        let (_, _, h, w) = x.dims4()?;\n        let factor = 1usize << self.num_blocks;\n        let pad_h = (factor - h % factor) % factor;\n        let pad_w = (factor - w % factor) % factor;\n        let x = x.pad_with_zeros(D::Minus2, 0, pad_h)?;\n        let x = x.pad_with_zeros(D::Minus1, 0, pad_w)?;\n        Ok((x, h, w))\n    }\n\n    pub fn forward(&self, x: &Tensor) -> Result<Tensor> {\n        let (x, orig_h, orig_w) = self.pad_to_fit(x)?;\n        let mut h = self.input_proj.forward(&x)?;\n        let mut skips = Vec::with_capacity(self.encoder_blocks.len());\n        for block in &self.encoder_blocks {\n            h = block.forward(&h)?;\n            skips.push(h.clone());\n        }\n        for block in &self.middle_blocks {\n            h = block.forward(&h)?;\n        }\n        for (block, skip) in self.decoder_blocks.iter().zip(skips.iter().rev()) {\n            h = (h + skip)?;\n            h = block.forward(&h)?;\n        }\n        let h = gelu(&self.head_conv1.forward(&h)?)?;\n        let h = self.head_conv2.forward(&h)?;\n        h.narrow(D::Minus2, 0, orig_h)?.narrow(D::Minus1, 0, orig_w)\n    }\n}"},{"key":"ci-2b77ed83-cc89c089-1","symbol":{"kind":"function_item","line_end":224,"line_start":216,"name":"forward","path":"crates/resound-core/src/aliasfree.rs"},"text":"crates/resound-core/src/aliasfree.rs:216:224 forward\n    pub fn forward(&self, x: &Tensor) -> Result<Tensor> {\n        let mut h = x.clone();\n        for (conv1, act, conv2) in &self.layers {\n            h = conv1.forward(&h)?;\n            h = act.forward(&h)?;\n            h = conv2.forward(&h)?;\n        }\n        x + h\n    }"},{"key":"ci-a6df9eaf-79ba275b-0","symbol":{"kind":"function_item","line_end":211,"line_start":178,"name":"main","path":"crates/resound-core/src/main.rs"},"text":"crates/resound-core/src/main.rs:178:211 main\nfn main() {\n    check_unet().expect(\"unet check failed\");\n    check_irmae().expect(\"irmae check failed\");\n    check_ampblock().expect(\"ampblock check failed\");\n    check_univnet().expect(\"univnet check failed\");\n    check_cfm().expect(\"cfm check failed\");\n    check_chunking();\n    check_real_weights().expect(\"real weight loading failed\");\n    check_edge_cases();\n    let sr = 16000usize;\n    let freq = 440.0f32;\n    let wav: Vec<f32> = (0..sr)\n        .map(|i| (2.0 * std::f32::consts::PI * freq * i as f32 / sr as f32).sin())\n        .collect();\n\n    let mel = MelSpectrogram::new(44100.0, 2048, 420, 2048, 128, 0.0, 22050.0, 0.97);\n    let resampler = Resampler::new(sr, 44100);\n    let resampled = resampler.process(&wav);\n    let mel_out = mel.forward(&resampled);\n\n    println!(\"resampled_len={}\", resampled.len());\n    println!(\"mel_bins={} mel_frames={}\", mel_out.len(), mel_out[0].len());\n    let peak_bin = mel_out\n        .iter()\n        .enumerate()\n        .max_by(|a, b| {\n            let sa: f32 = a.1.iter().sum();\n            let sb: f32 = b.1.iter().sum();\n            sa.partial_cmp(&sb).unwrap()\n        })\n        .map(|(i, _)| i)\n        .unwrap();\n    println!(\"peak_mel_bin={}\", peak_bin);\n}"},{"key":"ci-2b77ed83-cc89c089-0","symbol":{"kind":"impl_item","line_end":225,"line_start":194,"name":"","path":"crates/resound-core/src/aliasfree.rs"},"text":"crates/resound-core/src/aliasfree.rs:194:225 \nimpl AmpBlock {\n    pub fn new(channels: usize, dilations: &[usize], vb: VarBuilder, device: &Device) -> Result<Self> {\n        let mut layers = Vec::with_capacity(dilations.len());\n        for (i, &d) in dilations.iter().enumerate() {\n            let layer_vb = vb.pp(i);\n            let cfg1 = Conv1dConfig {\n                padding: d,\n                dilation: d,\n                ..Default::default()\n            };\n            let conv1 = conv1d(channels, channels, 3, cfg1, layer_vb.pp(\"0\"))?;\n            let act = UpActDown::new(channels, layer_vb.pp(\"1\"), device)?;\n            let cfg2 = Conv1dConfig {\n                padding: 1,\n                ..Default::default()\n            };\n            let conv2 = conv1d(channels, channels, 3, cfg2, layer_vb.pp(\"2\"))?;\n            layers.push((conv1, act, conv2));\n        }\n        Ok(Self { layers })\n    }\n\n    pub fn forward(&self, x: &Tensor) -> Result<Tensor> {\n        let mut h = x.clone();\n        for (conv1, act, conv2) in &self.layers {\n            h = conv1.forward(&h)?;\n            h = act.forward(&h)?;\n            h = conv2.forward(&h)?;\n        }\n        x + h\n    }\n}"},{"key":"ci-1e4a4f19-e0eb3216-0","symbol":{"kind":"impl_item","line_end":95,"line_start":43,"name":"","path":"crates/resound-core/src/resample.rs"},"text":"crates/resound-core/src/resample.rs:43:95 \nimpl Resampler {\n    pub fn new(orig_freq: usize, new_freq: usize) -> Self {\n        let g = gcd(orig_freq, new_freq);\n        let orig_freq = orig_freq / g;\n        let new_freq = new_freq / g;\n\n        let lowpass_filter_width = 64usize;\n        let rolloff = 0.9475937167399596f64;\n        let beta = kaiser_beta(lowpass_filter_width);\n\n        let base_freq = (orig_freq.min(new_freq)) as f64 * rolloff;\n        let width = (lowpass_filter_width as f64 * orig_freq as f64 / base_freq).ceil() as usize;\n\n        let mut kernel = vec![vec![0.0f64; 2 * width + 1]; new_freq];\n        for i in 0..new_freq {\n            let t = i as f64 / new_freq as f64 * orig_freq as f64;\n            for (j, k) in kernel[i].iter_mut().enumerate() {\n                let idx = j as i64 - width as i64;\n                let time = idx as f64 - (t - t.round());\n                let val = sinc(time * base_freq / orig_freq as f64) * base_freq / orig_freq as f64;\n                let window_arg = std::f64::consts::PI * beta\n                    * (1.0 - (time / width as f64).powi(2)).max(0.0).sqrt();\n                let kaiser = i0(window_arg) / i0(std::f64::consts::PI * beta);\n                *k = val * kaiser;\n            }\n        }\n\n        Resampler {\n            orig_freq,\n            new_freq,\n            kernel,\n            width,\n        }\n    }\n\n    pub fn process(&self, input: &[f32]) -> Vec<f32> {\n        let out_len = (input.len() * self.new_freq + self.orig_freq - 1) / self.orig_freq;\n        let mut out = vec![0.0f32; out_len];\n        for i in 0..out_len {\n            let t = i as f64 / self.new_freq as f64 * self.orig_freq as f64;\n            let center = t.round() as i64;\n            let mut acc = 0.0f64;\n            for (j, &k) in self.kernel[i % self.new_freq].iter().enumerate() {\n                let src_idx = center + (j as i64 - self.width as i64);\n                if src_idx >= 0 && (src_idx as usize) < input.len() {\n                    acc += k * input[src_idx as usize] as f64;\n                }\n            }\n            out[i] = acc as f32;\n        }\n        out\n    }\n}"},{"key":"ci-95452ccc-ae1cca25-0","symbol":{"kind":"function_item","line_end":126,"line_start":124,"name":"normalize_db","path":"crates/resound-core/src/melspec.rs"},"text":"crates/resound-core/src/melspec.rs:124:126 normalize_db\npub fn normalize_db(db: f32, min_level_db: f32) -> f32 {\n    (db - min_level_db) / (-min_level_db + 15.0)\n}"}]},"dispatch_id":"1786111729817-132-9e540c4e958660e0","next_dispatch_hint":"instruction","ok":true,"request_fingerprint":"abf459c9e7164a87","verb":"codesearch"}